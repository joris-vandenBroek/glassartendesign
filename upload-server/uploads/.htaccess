# Uploaded files are served as static assets only -- never executed as scripts,
# even if a malicious file slips past the server-side image validation.
php_flag engine off
Options -ExecCGI -Indexes
<FilesMatch "\.(php|php\d|phtml|pl|py|cgi|sh)$">
    Require all denied
</FilesMatch>
